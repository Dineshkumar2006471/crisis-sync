{
  "rules": {
    ".read": false,
    ".write": false,
    "live_incidents": {
      ".read": false,
      ".write": false
    },
    "presence": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    }
  }
}
