{
  "rules": {
    ".read": false,
    ".write": false,
    "live_incidents": {
      ".read": "auth != null",
      "$incidentId": {
        ".write": "auth != null"
      }
    },
    "presence": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    }
  }
}
